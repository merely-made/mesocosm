[package]
name = "parry-ground"
version = "0.0.1"
edition = "2024"
license = "MPL-2.0"
publish = false

[workspace]

[dependencies]
mesocosm-core = { path = "../../mesocosm-core" }
parry3d = { version = "=0.29.0", features = ["enhanced-determinism"] }
