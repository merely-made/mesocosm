# design_docs Index

Canonical index for `design_docs/`. Per DOC_POLICY §5, this file wins over
any other index and is updated in the same session as any doc change.

## Working principles for AI assistants

- Read `../CLAUDE.md` first for repo role, terminology, and don'ts.
- Verify claims against the codebase and the sibling repos, not doc-to-doc
  consistency. This wing's founding record was corrected once already for
  trusting a stale index line over the plan it indexed.
- Plans carry done-conditions, not time estimates.
- `PROJECT_DESCRIPTION.md` is maintainer-owned; surface contradictions, do
  not edit unasked.
- The substrate/system split is load-bearing across the whole wing: one
  substrate, many rule-dressings. Keep it that way in every doc.
- The three pipeline laws (choices-not-morphology, pointable inheritance, no
  homework) govern anything that crosses between games. A violation is a
  design bug, not a preference.

## Active docs

| Doc | What it is |
| --- | ---------- |
| [DOC_POLICY.md](DOC_POLICY.md) | Documentation governance |
| [PROJECT_DESCRIPTION.md](PROJECT_DESCRIPTION.md) | Product goals and pillars (maintainer-owned): the mesocosm names the enclosure scale, whose roster can range from one-trait cells through complex and fantastical organisms. |
| [2026-07-30_games_wing_founding.md](2026-07-30_games_wing_founding.md) | **Wing-level, cited by the sibling games.** The three vessels and their care granularities (species / individuals / community), **the invariant being care granularity rather than person purity** with its three replacement guardrails and the design space that relaxation opens, the lifecycle and the restated anti-Spore law, the three pipeline laws, worlds-as-moots with fili/tulpa/gemot mapping and graduated interop, layered critter/character/place/faction profiles carried as engrams, signed multi-writer facts with per-domain materializers rather than a universal CRDT, the MPL/permissive-library/CC-assets license split, the verified stack, the two-game proof pair, Voxatron as the material-language lodestar, and the ruling that anatomy topology is portable identity while capability remains vessel-owned. **Amended 2026-08-10 (R4):** each vessel is a mode of the same peopled history — Isometry the fortress and atlas, Paredros the adventure, Mesocosm the ecology beneath both — with the DF limit binding (shared history, never a shared running world), facts crossing by the pipeline laws while verbs never cross, platform organs going up the stack rather than sideways into wing crates, and identity shared as one vocabulary while the consequence grammar stays sovereign per vessel. |
| [2026-07-30_mesocosm_founding_plan.md](2026-07-30_mesocosm_founding_plan.md) | Vessel 1's own design and phases: metabolize, incorporation-only parts with provenance, trait count as the real axis, an Exocosm-informed world-condition grammar including proto/soup and impossible worlds, the three trophic strategies, metabolic costs and material flow, **the epoch loop** with complex lineages committing first and simpler lineages adapting later, complexity-frontier switching while inactive lineages keep evolving, checked prior art, ecological competition, storyteller, death, and phases M0–M5. Updated to distinguish the completed v0 projection proof from the pending v1 identity contract. |
| [2026-07-30_engine_and_render_lane_landscape.md](2026-07-30_engine_and_render_lane_landscape.md) | **Historical candidate landscape**, last updated 2026-08-06. The verified pure-Rust engine field; renderling as lead mesh tenant; kiss3d as donor; the host-skeleton versus game-runtime distinction; V0-V2 projection proofs; `vello_hybrid`; capability profiles; and the tenancy cohesion contract. Its candidate labels do not override the later resident-views engine ruling or the 2026-08-18 critical review. |
| [2026-08-07_dependency_ledger.md](2026-08-07_dependency_ledger.md) | **Authority on ordering, corrected against live code 2026-08-26.** PD1b remains the next ProcessDef/product step rather than the global engine order; G0-G4 are historical completed build labels. Projection-correct equal-sized residency and the brick ABI/DDA ownership lift (the platform crate now named `modulus`, 2026-08-28) are complete. Shared-engine consolidation is closed through D1 (raymarch/renderling depth), T1 (terrarium picking through Conatus), and V1b (stable `ResidentChunk`-backed brick cache with per-brick publication, tracer-validated lease epochs, and allocator-observed bytes), all 2026-08-26. Product acceptance gates and the two-consumer promotion rules remain independently binding. |
| [2026-07-31_execution_waves_plan.md](2026-07-31_execution_waves_plan.md) | **Demoted to Wave 1/2 history** (audit 2026-08-07); superseded by the dependency ledger. Original description: the governing plans own the *what*, this owns the *when*. **Wave 1 complete**: `mesocosm-core` owning state while hosts only project, the Genet host, and the Isometry projection coupling by data rather than types. The Bevy host and the Renderling probe were **dropped** once the render lane was decided. Wave 2 in progress: 2.2 (epoch and ecology lab, three authored worlds) and 2.4 (the proof pair, Law C demonstrated in the consumer) are **complete**; 2.1 playfeel and 2.3 Paredros social proof remain. Carries the finding that the adaptation phase converges by round six with zero extinctions, because income is flat and uncontested. |
| [2026-07-31_phenotype_plan.md](2026-07-31_phenotype_plan.md) | **Mesocosm body decisions and proof plan.** Unifies the special played body with scalar organisms without requiring every distant life form to stay voxel-realized; separates structural dependency from cyclic functional links, somatic growth from filial adaptation, and anatomy from vessel capability; rules that adaptation previews a founder phenotype but commits a developmental program; keeps the trait array until a real replacement exists; puts burn-versus-incorporate first; derives adaptation income from local finite flows; and orders P0-P6 with explicit stop rules. |
| [2026-08-01_epoch_boundary_plan.md](2026-08-01_epoch_boundary_plan.md) | **Design plan, partly built.** What happens *between* epochs. Speciation is an act rather than a threshold: the player forks and names, an unplayed lineage splits on a significant event, and the split *is* the reward. Shared-lineage adaptation uses immutable revisions and adopt-or-branch authorship: agreement adopts one developmental program, disagreement preserves descent instead of merging edits, and realized descendant bodies may differ. Significance is abnormality measured against the world's own record, judged retrospectively at the epoch boundary across seven axes, which gives the world a difficulty curve nobody authored and gives the storyteller a real brief: keep significance achievable by opening possibility. Selected goals are the aimable half and deliberately do not collapse into the retroactive half. Temperament is biographical, and plasticity is a life stage that youth pays for in capability. The event log, species tree, world record, and place graph have landed; NPC speciation and the full boundary experience remain. |
| [2026-08-01_processdef_plan.md](2026-08-01_processdef_plan.md) | **Mesocosm's extensible biology lane.** Defines `ProcessDef` as a namespaced transformation rather than a universal gene or ability flag; separates acquisition, developmental expression, embodied process allocation, channels, capability, and ecology. PD1a now rules a transactional `BodyPhenotype`, authoritative per-part cell graphs, one validator for direct and automatic arrangement, structural capacity versus current availability, and carry-body versus regrow-here crossing. Records both the recipe-to-authoritative-body proof and the landed live constructor join across founding, birth, local regrowth, and founder preview; PD1b is next, followed by one native played process before pack and Piccolo infrastructure. |
| [2026-07-31_wing_phenotype_contract_plan.md](2026-07-31_wing_phenotype_contract_plan.md) | **Wing-level body identity contract.** Separates subject, biological line, body revision, part address, structural anatomy, optional functional links, phenotype, projection, and chronicle; rules that primitive topology travels while every vessel derives its own capabilities; distinguishes the same individual from a descendant and carry-this-body from regrow-here crossing; specifies branch provenance, authority under concurrent facts, the body/chronicle v1 split, and W0-W6 proof gates. |
| [2026-08-02_views_founding_plan.md](2026-08-02_views_founding_plan.md) | **Mesocosm's UI founding.** Adapter-first: `mesocosm-views` discloses world facts as sceno scores, scenomise solves, sprigging leaves realize; a layout computed in views is in the wrong crate. First chrome is the **minimap** (which lineage dominates each region), consuming `Arrangement::Hulls` whose cells match `Places::at` exactly (the congruence test). Region meaning stays per-vessel; the backdrop is a dynamically generated DOM layer. Hosts are **staged**: route A (textless HUD lane: leaf → paint_list_render → netrender on the shared device → overlay blend) landed 2026-08-02 with the minimap on screen (`14_minimap.png`), and shook a premultiply bug out of paint_list_render; route B (cambium lane) waits for the epoch-boundary screen, and the HUD never grows a text hack. The generated backdrop landed the same day (the world rendering its own enclosure top-down on a cadence, under the translucent cells; `15_backdrop.png`), surfacing the vello display-encoded-output seam (sRGB twin copy). Interactivity and fields-over-cells open. |
| [2026-08-05_place_graph_engine_plan.md](2026-08-05_place_graph_engine_plan.md) | **The active world-substrate record.** Records the 2026-08-05/06 fundamentals rulings (one authority per capability; shared engine organs with sovereign verbs; volumetric truth permitted; verticality as a simulation affordance; distinct world graphs; elective continuous nesting; hybrid worldgen; per-vessel design reference targets; **three-tier physics**: integer authority / parry+owned kinematics advisor / GPU ambience, rapier in reserve; renderer tenancy per the cohesion contract; **Mesocosm camera pulled back Rain World-style 2026-08-06**, the brick tracer's primary consumer becoming Paredros). The model is a place graph indexing one continuous voxel space, brick truth per region, and two-tier simulation with the existing ecology as the far tier. G0-G4 are now historical build labels rather than acceptance gates. P0-P8 compose generated Ground, player and autonomous crossing, replay, body projection, native/Browser WebGPU, netrender, and population cost in one burrow run. **P9-P10 are implemented locally 2026-08-21:** live body geometry constrains locomotion, tunnel growth, and terrain sight, producing compact/broad passage divergence and compact/tall perception divergence through the ordinary ecology policy. |
| [2026-08-06_general_model_plan.md](2026-08-06_general_model_plan.md) | **One model, two configurations.** Research and founding. Records a verified prior-art ledger (GEM, speculative evolution, computational modeling of religion, thaumatology), the empty-slot finding (no name coined), and the 2026-08-07 review's two load-bearing corrections: **one causal grammar with sovereign evaluators** (never one model with settings), and the skeleton's fifth layer, **transition**, where the game actually lives. Ecological half **E0–E4 slices landed 2026-08-07; acceptance gates open (audit)** (allometry, predation via satisfied feeding modes, dispersal with recorded focus and hysteretic tiers, cohort conservation slice, drives replacing the Hunter), with review-accepted follow-up receipts per gate; the changed principle — categorical traits select mechanisms, continuous traits modulate rates — plus the concrete bug that linear upkeep imposes an unchosen size ceiling, feeding mode derived from anatomy, cohorts as the far tier's unit, and the `Hunter` FSM dissolving into drives. Fantastical half **proposed only, proof-first after review** (F0 = one concrete vertical slice; extraction follows repetition): typed state carriers (quantity/field/condition/relation-mark/claim) under an eight-axis questionnaire, one effect envelope proposing typed changes, derivation by signatures/similarity/contagion over existing trait vectors and `Provenance` edges, kleptoplasty extended past biology by criteria, discovery as the delivery vehicle, and a fixed-Technique/generated-Form grammar. Carries the Ultima Online caution (simulated layers die to performance and comprehension; keep state legible even if dynamics are throttled) and an explicit verification-debt list. |
| [2026-08-18_vessel_briefs_and_presentation.md](2026-08-18_vessel_briefs_and_presentation.md) | **The ratified vessel briefs and the camera rulings** (2026-08-18). Isometry: procedural voxel VTT, pluggable rulesets, PF2e/SRD campaign end-to-end as first release, Role/Creator modes. Paredros: voxel Kenshi, third-person continuous zoom, first person as a Paredros-only setting. Mesocosm: ecological roguelike, **side-on terrarium section** + trait-graph review board (closes the Rain World pull-back). Shared-world fidelity = metadata broad strokes and motifs, never pixel consistency; the primordial-name hook; **arc / unprecedented event / epoch modifier as one record-first mechanism at three timescales** (the architected answer to Kenshi reactivity); alignment as a ruleset binding with commitments (edicts/anathema) as the arc primitive, axes-as-data per world. Closes the founding record dimensionality carry; corrects the CLAUDE.md identity line. |
| [2026-08-14_resident_views_composition_plan.md](2026-08-14_resident_views_composition_plan.md) | **How the voxel world composes with Burn/CubeCL, the tracer, and collision on one device.** Ratified 2026-08-14: voxel-authoritative and Burn-addressable; `ResidentChunk` as the zero-copy seam; **Burn proposes, the record disposes**. Carries the 2026-08-16 hybrid engine ruling: CubeCL-authored compute, brick raymarch ground, renderling mesh tenancy, netrender composition, Parry tactile projection, nexus gated on dynamic bodies, hecs as runtime-index-never-record, and residency as the remaining new system. **Lanes A-F and the seed crystal are complete.** C earned the CubeCL-JIT consolidation verdict against the real incumbent control: 11.39 ms versus 15.84 ms. E's Ground histogram is pinned; F's GPU `LeasedAtlas` source is tracked; the seed crystal composes growth and DDA over one resident allocation. The tracked `resident_ground` receipt composes a Burn proposal, constrained `Ground` commit, retained atlas patch, strided brick lease, visible tracer result, and replay. Mesocosm also consumes Conatus voxel mechanics through `GroundVoxelProfile`. **Projection cache coherence and shared traversal ownership closed 2026-08-26:** equal-sized pages require explicit projection revision, retain textures while fully republishing changed contents, reject mismatched leases, and keep unchanged frames silent; Mere's `modulus` (renamed from `conatus-brick` 2026-08-28) now owns their deterministic ABI and camera-neutral DDA while products retain bindings and presentation; its voxel-mechanics sibling is now `nisus`. Headless coverage and the headed Paredros equal-extent travel receipt are green. V1b (2026-08-26) closed incremental per-brick residency, allocator-observed bytes, and the lease epoch: retargets publish loaded slots into retained allocations and the tracer validates the frame's expected epoch. Field-plane visualization remains open. |
| [2026-08-28_played_slice_plan.md](2026-08-28_played_slice_plan.md) | **Active: the first played slice; PS0 and PS1 landed 2026-08-28.** The mesocosm-genet host plays the live World in the brick-traced side-on terrarium section under the ruled pull-back camera, with trace-on-exit and replay-identical receipts (the hash assertion proven falsifiable). Open: the section's half-height framing ruling, and a lens pose roster so organisms besides the played one appear in the section. D5's burn/incorporate choice deferred pending an interaction design (hotkeys rejected by Mark 2026-08-28); succession and the epoch boundary extracted to PS2. |
| [2026-08-18_engine_ecology_rulings_and_review.md](2026-08-18_engine_ecology_rulings_and_review.md) | **Provisional engine consequences and critical review, refreshed through 2026-08-26.** Preserves record authority, one resident allocation path, hybrid raymarch-plus-renderling composition, Conatus-private tactile realization, and evolved controllers as bounded intent proposers. Corrects the overclaims: shared render chunks are narrowed rather than abolished; DDA traversal is reusable but renderer policy is vessel-owned; Mesocosm is cheaper than Paredros rather than nearly free; the GPU-only renderling bake seam includes allocation, lifetime, count, ABI, and draw receipts; nexus is ruled decomposed-into-Conatus (2026-08-28), gated on the first dynamic-body consumer. R1 traversal, R2 GPU mesh bake, R3 committed collision, B1 bounded fauna policy, V1 continuous-zoom residency, V1a projection identity, the brick platform ownership lift (now `modulus`), D1 raymarch/renderling depth composition, T1 terrarium picking through Conatus, and V1b's stable resident brick cache are complete. The exact-revision consumer suites and headed equal-extent rerun are green, with a byte-identical PNG after extraction. The ordered shared-engine chain is closed; remaining spatial contracts are consumer-forced. Clipmaps and compact procedural vertices remain consumer-gated. |

## Archive

- `archive_docs/2026-08-07/2026-07-30_body_pipeline_and_host_probe_plan.md`
  — archived 2026-08-07: R0/R1/R3 complete, R2 superseded, R4 transferred
  to the Paredros execution plan (fires after S3).


None yet. Retired plans go to `archive_docs/<YYYY-MM-DD>/`.
